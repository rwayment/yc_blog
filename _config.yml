# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.


title: "Yukon Conelius's Hobby Blog"
email: coneliusyukon@gmail.com
description: >-
  This is the Hobby Blog for Yukon Conelius. I write about 
  my hobbies and interests, which include programming, gaming, and music.
baseurl: "/"
url: "https://the-island-of-misfit-minis.blog"

theme: "minimal-mistakes-jekyll"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-archives
  - jekyll-paginate
  - jekyll-include-cache

markdown: kramdown
show_excerpts: true
excerpt_separator: "<!--more-->"

# Minimal Mistakes settings
minimal_mistakes_skin: "default"
search: true
paginate: 5
paginate_path: "/page:num/"
permalink: /:categories/:year/:month/:day/:title/

# Home page settings
home_paginate_show_teasers: true

# Archives
collections:
  posts:
    output: true
    permalink: /:categories/:year/:month/:day/:title/

defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: single
      author_profile: true
  - scope:
      path: ""
    values:
      entries_layout: grid

# Sidebar
sidebar:
  nav: "sidebar-nav"

# Tag and archive settings
tag_archive:
  type: liquid
  path: /tags/
category_archive:
  type: liquid
  path: /categories/


# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
# Standard excludes for Jekyll
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
